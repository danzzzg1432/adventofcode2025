const { btoa } = require('buffer');
const fs = require("fs")
const input = fs.readFileSync('input.txt', 'utf8');



function main() { 
    testInput = input.trim().split("\n") // splits by newline by default
}


main()